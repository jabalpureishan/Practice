#include <stdio.h>
#include <stdlib.h>
int main(){
    int x=0,y=1,num;
    scanf("%d",&num);
    for(int i=0;i<=num;i++){
        int temp = y;
        printf("%d",&y);
        y = x+y;
        x = temp;
    }
    return 0;

}